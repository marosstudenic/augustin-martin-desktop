---
import { getLangFromUrl, useTranslations } from "../i18n/utils";

import { Image } from "astro:assets";
import img1 from "../assets/uvod/MA_uvod1.png";
import img2 from "../assets/uvod/MA_uvod2.png";
import img3 from "../assets/uvod/MA_uvod3.png";
import img4 from "../assets/uvod/MA_uvod4.png";
import img5 from "../assets/uvod/MA_uvod5.png";

import video1 from "../assets/videos/1.png";
import video2 from "../assets/videos/2.png";
import video3 from "../assets/videos/3.png";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<style scoped>
    #myImg {
        border-radius: 5px;
        cursor: pointer;
        transition: 0.3s;
    }

    #myImg:hover {
        opacity: 0.7;
    }

    /* The Modal (background) */
    .modal {
        display: none;
        /* Hidden by default */
        position: fixed;
        /* Stay in place */
        z-index: 50;
        /* Sit on top */
        padding-top: 50ppx;
        /* Location of the box */
        left: 0;
        top: 0;
        width: 100%;
        /* Full width */
        height: 100%;
        /* Full height */
        overflow: auto;
        /* Enable scroll if needed */
        background-color: rgb(0, 0, 0);
        /* Fallback color */
        background-color: rgba(0, 0, 0, 0.9);
        /* Black w/ opacity */
    }

    /* Modal Content (Image) */
    .modal-content {
        margin: auto;
        display: block;
    }

    /* Caption of Modal Image (Image Text) - Same Width as the Image */
    #caption {
        margin: auto;
        display: block;
        width: 80%;
        max-width: 700px;
        text-align: center;
        color: #ccc;
        padding: 10px 0;
        height: 150px;
    }

    /* Add Animation - Zoom in the Modal */
    .modal-content,
    #caption {
        animation-name: zoom;
        animation-duration: 0.6s;
    }

    @keyframes zoom {
        from {
            transform: scale(0);
        }

        to {
            transform: scale(1);
        }
    }

    /* The Close Button */
    .close2 {
        position: absolute;
        top: 15px;
        right: 35px;
        color: #f1f1f1;
        font-size: 40px;
        font-weight: bold;
        transition: 0.3s;
    }

    .close2:hover,
    .close2:focus {
        color: #bbb;
        text-decoration: none;
        cursor: pointer;
    }

    /* 100% Image Width on Smaller Screens */
    @media only screen and (max-width: 700px) {
        .modal-content {
            width: 100%;
        }
    }
</style>

<section id="uvod" class="pb-12 md:pt-60">
    <div class="block md:flex">
        <Image
            id="intro1"
            alt=""
            class="w-full object-cover hidden h-[540px]"
            src={img1}
        />
        <Image
            id="intro2"
            alt=""
            class="w-full object-cover hidden md:w-1/2 h-[540px]"
            src={img2}
        />
        <Image
            id="intro3"
            alt=""
            class="w-full object-cover hidden  md:w-1/2 h-[540px]"
            src={img3}
        />
        <Image
            id="intro4"
            alt=""
            class="w-full object-cover hidden md:w-1/2 h-[540px]"
            src={img4}
        />
        <Image
            id="intro5"
            alt=""
            class="w-full object-cover hidden md:w-1/2 h-[540px]"
            src={img5}
        />

        <div class="flex-1 py-4 px-4">
            <div class="text-gray-500 text-sm pr-8">
                <h2 class="font-medium text-2xl">{t("prologue.title")}</h2>
                <p class="my-4">
                    {t("prologue.p1")}
                </p>
                <p class="my-4">
                    {t("prologue.p2")}
                </p>
                <p class="my-4">
                    {t("prologue.p3")}
                </p>

                <div class="flex justify-end">
                    <span style="float: right">{t("prologue.author")}</span>
                </div>
            </div>
        </div>
        <div class="flex-1 flex">
            <div class="container max-w-[500px] my-auto px-2 mt-8">
                <Image
                    src={video1}
                    alt=""
                    id="img1"
                    class="rounded-md"
                />
                <p class="text-gray-600 text-sm my-2">
                    {t("prologue.video1")}
                </p>
                <div class="w-full md:flex my-2 gap-2">
                    <div class="flex-1 my-8 md:my-0">
                        <Image
                            src={video2}
                            alt=""
                            id="img2"
                            class="w-full rounded-md"
                        />
                        <p class="text-gray-600 text-sm my-2">
                            {t("prologue.video2")}
                        </p>
                    </div>
                    <div class="flex-1">
                        <Image
                            src={video3}
                            alt=""
                            id="img3"
                            class="w-full rounded-md"
                        />
                        <p class="text-gray-600 text-sm my-2">
                            {t("prologue.video3")}
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div id="myModal2" class="modal w-full px-12">
        <!-- The Close Button -->
        <span class="close2">&times;</span>

        <!-- Modal Content (The Image) -->
        <iframe
            src=""
            frameborder="0"
            id="iframe"
            class="w-full h-[80vh] mt-20"
            title="welcome video"></iframe>

        <!-- Modal Caption (Image Text) -->
        <!-- <div id="caption"></div> -->
    </div>
</section>

<script>
    const pairs = {
        img1: {
            image: "videos/1.png",
            iframe: "https://www.youtube.com/embed/jjzlpjNv6FI",
        },
        img2: {
            image: "videos/2.png",
            iframe: "https://www.youtube.com/embed/lI_hCfHcRrg",
        },
        img3: {
            image: "videos/3.png",
            iframe: "https://www.youtube.com/embed/AbXn9KBMz9c",
        },
        img4: {
            image: "videos/4.png",
            iframe: "https://www.youtube.com/embed/ep8bOUfaS24",
        },
    };

    function showModal(iframeUrl) {
        var modalFrame = document.getElementById("iframe");
        // var captionText = document.getElementById("caption");
        modal.style.display = "block";
        modalFrame.src = iframeUrl;
        // captionText.innerHTML = img.alt;

        // Get the <span> element that closes the modal
        var span = document.getElementsByClassName("close2")[0];

        // When the user clicks on <span> (x), close the modal
        span.onclick = function () {
            modal.style.display = "none";
        };
    }

    // Get the modal
    var modal = document.getElementById("myModal2");

    // Get the image and insert it inside the modal - use its "alt" text as a caption
    document.getElementById("img1").onclick = (el) => {
        showModal(pairs[el.target.id].iframe);
    };
    document.getElementById("img2").onclick = (el) => {
        showModal(pairs[el.target.id].iframe);
    };
    document.getElementById("img3").onclick = (el) => {
        showModal(pairs[el.target.id].iframe);
    };

    function selectIntroImage() {
        // choose random number between 1 and 5
        let random = Math.floor(Math.random() * 5) + 1;
        let name = "intro" + random;
        console.log(name)
        let img = document.getElementById(name);
        img.classList.remove("hidden");
    }

    selectIntroImage();
</script>
